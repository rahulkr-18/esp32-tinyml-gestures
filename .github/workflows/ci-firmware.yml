name: Firmware Build CI
on: [push, pull_request]

jobs:
  lint-check:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Setup Python 3.11
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Check firmware folders exist
      run: |
        test -d firmware_micropython && echo 'micropython folder OK'
        test -d firmware_tflite && echo 'tflite folder OK'
        echo 'Firmware structure check passed'
